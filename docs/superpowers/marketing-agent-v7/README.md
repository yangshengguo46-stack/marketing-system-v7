# Marketing Agent v7 文档与施工入口

本目录同步本次对话已经交付的文档。**当前实施依据是 v1.2 业务与架构融合版**；旧版按原样归档，不构成与 v1.2 平行的另一套实施计划。

## 阅读顺序

1. [业务与架构融合章节](v1.2/BUSINESS_ARCHITECTURE.md)
2. [完整目标规格](v1.2/FINAL_SPEC.md)
3. [修订后的施工任务单](v1.2/IMPLEMENTATION_TASKS.md)
4. [来源与源码核对索引](v1.2/SOURCE_MAP.md)
5. 当前工作包对应的 [合同草案](v1.2/contracts/) 与 [原型测试](v1.2/tests/)

供 Coding Agent 使用的交接提示词见 [CODING_AGENT_PROMPT.md](CODING_AGENT_PROMPT.md)。请在读取完本入口后进入真实源码核对，不只再交付一份替代架构。

## 已确认的产品决定

继续在第七版仓库、同一个 Lead、原生 Codex 模型—工具循环上实现。Core 允许修改，不再要求持续合并上游；保留作用域、事实、授权、预算、取消和恢复边界。业务判断采用具体角色、处境、变化、事件、视角、候选与营销干预，并复用现有编剧课程及候选创作方法，而不是建立固定营销步骤或第二个营销大脑。

业务与工程同步推进：从 WP00+B00 开始，将 B01/B02 接入 WP01–03 的最小条件，尽早完成 WP04+B03 的首次完整作品。不要等全部数据库、平台和界面建完才补业务，也不要要求客户先教会产品营销。

## 版本与历史范围

- `v1.2/`：最新完整融合包，所有原始文件按字节保留。
- `archive/v1.0/`：早期工程实施包，仅历史参考；不重新执行其中已被 v1.2 取代的业务后置安排。
- `archive/v1.1/`：场景营销补充规格，后续已融入 v1.2。
- `archive/discussion/`：用户提供的早期“掌管营销的神”原稿。保留业务思想与讨论来历；不恢复其中已被取代的外置 Marketing Host、核心营销 MCP、固定候选数、强制阶段和“先不交付正文”的旧接法。

原始文档中的“本版不修改 GitHub 仓库”“本次未推送”等，是该文档生成时的历史状态，并非本次文档同步操作的状态声明。同步文件不等于实施产品。

## 验证与授权边界

新增接口、SQL、配置与 Rust 草案是目标合同或原型，不是已编译集成的产品代码。`BASELINE_v1_0_VALIDATION_LOG.txt` 等日志是原包的历史记录，不是本次重新运行的验收，不证明业务增益、模型能力或上线状态。

本次文档同步只允许增加本目录，不修改 `codex-rs/`、`ai-ip-assets/`、现有 AGENTS、课程正文、仓库历史或运行配置。产品实现、付费调用、公开发布、破坏性迁移和代码合并按相应明确授权执行。

文档核对基线为 `e08f1e1ad5bbc3ea036aa271894d8228e499ed61`。施工前核对当前分支、HEAD 和未提交修改；不为匹配旧基线而回退或覆盖用户工作。

## 文件完整性

[SYNC_MANIFEST.json](SYNC_MANIFEST.json) 记录 38 个原始文件的目标路径、来源、字节数、SHA-256 与 Git blob SHA-1。可在本目录运行 `python verify_sync.py` 进行离线完整性检查。该检查不导入或运行合同中的测试程序，也不连接模型或平台。
