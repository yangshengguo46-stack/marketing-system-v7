# 只整理交付并核对，不重写故事

原生v2工作文件与原稿已经另存。根Lead回看真实工具事件发现：v2自查称回读三阶段完整body，实际只返回首尾120字符和检查关键词；draft仍先公式再场面，与题目要求相反。另有notes中读者感受断言未改，已由根Lead另存 notes-v3-editorial.md 校正，明确本地编辑身份，原生v2未覆盖。请修复这些实际执行问题，不重写故事或扩建系统。

1. 通过 marketing_work 重新打开已有 workId=charlie-method-v1-homework34，只传action=open与workId，不传新brief/materials/startAt，避免上轮的冲突错误。
2. 通过exec_command分别输出该原生JSON三个results的完整body（index 0/1/2），每次显式max_output_tokens=6000，检查返回没有真正截断标记。不要用首尾片段、长度或关键词代替完整读取。只读本工作文件，不能探索目录。
3. 完整读本目录 scene-v2.md 和 notes-v3-editorial.md（分别读，max_output_tokens=6000）。scene是原生作者的完整修后正文；notes-v3是根Lead本地措辞校正，不能说是你或原作者自动改好。
4. 用 marketing_work record draft 保存body：先逐字放 scene-v2.md 全文，接分隔线，再逐字放 notes-v3-editorial.md 全文。不添加新剧情、评价或自查胜利宣言。必须保留notes中的本地编辑说明。不改变research/direction、旧文件及它们的历史。原生record写入这份已有编辑稿，不是新的创意实跑或强模型对照。
5. 再用exec_command完整输出最新draft body，max_output_tokens=6000；同时用Python比较 body == scene-v2.md全文 + 你实际使用的明确分隔 + notes-v3-editorial.md全文，输出真假（整串等值，不是关键词存在）。原文开头必须是scene，不是公式。若失败按差异修复原生record，不用shell覆盖原生JSON。
6. 在本目录 delivery-verification-v3.md 写实际操作、校验结果、原生v2的假自查与本地notes校正身份、仍未知（质量/读者效果/稳定自主执行未通过）。本报告不能再次把未做的检查写成通过。完成后停止，不生成视频或重写scene。
