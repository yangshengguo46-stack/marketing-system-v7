# 原生前两轮证据检查

统计截至第二次 `turn/completed`（事件第 10456 行，2026-09-06 12:38:16 UTC），排除第三轮。原生请求与服务端报告模型均为 `glm-5-2-260617`，两轮状态均为 completed、error=null。

- 32 次原生函数调用均有对应输出；其中 `marketing_work` 7 次正常、0 次失败，另 25 次命令均成功退出。
- 两轮各自 research/direction/draft 的正文与其 record 参数逐字一致；v1/v2 Markdown 仅比各自 draft 多一个末尾换行。v1 正文 1556 可见字符、1336 中文字符；v2 为 1964/1685。
- 13 次完整读取有 `cat` 输出和存档哈希支持：v1 读入口、题目、第4/5课页、故事方法、第一份故事路线；v2 读评阅、返工要求、三份方法、第34课页及v1全文。未见截断。这里指选用课程页全文，不是92段原始转录全读。
- 修改 research 后的状态为 `[false,true,true]`，再修改 direction 为 `[false,false,true]`，写入新 draft 后为全 false。事件第 9548/9553/9569 行直接记录了失效传递。
- 故事方法实际读取版本不同：v1 哈希 `75c8fc58…`；v2 为 `0476ac25…`，增加了顺序检查物件、承诺、时间与代价的段落。不把之后新增的计算提示倒记为v1已读。

需纠正的自报：

1. v2 说“v1原生记录保留在workFile JSON中”不成立。该文件仅有三个当前阶段值；v1历史在外部状态快照、正文和事件中，不能称工具自动保留历史。
2. v2 自称时间计算已准确，正文仍有 `3:52+5=4:00` 和 `3:59+12=4:12` 的矛盾。保存一致和执行成功不等于故事逻辑通过。
3. v1 自称“分段读取”，实际是先看行数，再完整 `cat`；完整、未截断成立，分段的表述不准确。

审计读取当前 workFile 时它已进入v3，故不将它与v2快照不同列为前两轮保存错误，也不评价第三轮。未读取stderr、配置或凭据；未捕获或声称捕获HTTP请求。详细callId、事件行号、哈希及版本比较见 [JSON审计](native-v1-v2-audit.json)。
