# 完整创作与整体返工：2026-09-09

用户已授权沿主线实施。沿用虚构修鞋题及原生作业 `course-full-story-20260906`，在新的原生线程中重新打开；原 v1–v3 与此前资料保留。目标是完整作品中的整体表达与实质返工，不制作视频，不延续已搁置的亚特兰蒂斯取材或竞争产品调研。

本轮沿用现有 App Server 与 `native_client.py`、授权方舟 GLM。提供的课程核心方法已含修鞋题的编辑提示，因此属于有针对性材料辅助的单题试用，不是模型无提示独立发现，也不是与通用智能体的公平对照。

原始输入副本见 `inputs/`，摘要见 `inputs-before.json`；旧原生工作状态见 `work-before-v4.json`。实际原生事件位于 `../../output/course-learning/charlie-20260906/run/whole-story-20260909-events.ndjson`。本轮已收尾，事实与边界如下。

## 实际成果与判断

- [v4 完整稿](../../output/course-learning/charlie-20260906/练习/第一份故事-v4.md)：正文1849汉字，加标题1853；建立女儿因上次缺席而不再催促的关系背景，但主要行动仍沿用旧稿。[仅看正文的独立读稿](v4-reader-feedback.md)能读到父女信任，同时指出冲突很快化解、衣服支线无依据、解释过多。
- [v5 完整返工稿](../../output/course-learning/charlie-20260906/练习/第一份故事-v5.md)：正文1523汉字；删除衣服支线和重复解释，增加顾客催促、碰纸袋、复查鞋的行动。[定向复核](v5-reader-feedback.md)指出父亲再次报时并失约，正文与作者“开始说实话”的判断不一致。**不记为质量通过**，没有用户或真人读者认可。
- 原生工具实际重新打开同一 workId、更新研究/方向/正文；上游更新保留旧稿并标待复核，两份正文与当时原生 draft 一致，仅末尾换行有差异。旧工作状态、中断时状态、v4/v5状态单独留存；当前工作JSON不会自动保存所有旧版本。工具的 `needsReview=false` 只表示阶段已记录，不能代表上述作品问题已通过。

## 执行与输入问题

原生线程 `01a085ab-7650-7e72-9941-5a8fa19e4cf3`，`glm-5-2-260617`。首次 medium 轮652.746秒后由Lead中断，届时研究已保存，尚无v4正文；其后请求 low，136.504秒完成v4，359.265秒完成v5。两次完成、一轮中断；7次原生营销工具调用成功，23次文件/计算命令exit 0。low是请求的档位，不据此证明供应商实际推理预算受控或它导致速度变化。

核心方法和旧工作JSON返回出现截断，Lead两次原生steer要求补读。核心方法缺段、旧研究和方向补读后完整；旧工作笔记仍未读全，已明确不据此宣称完整输入。入口、题目、v3、三份具体方法、核心方法及旧v3复核的正文在实际返回中可核对；没有把完整课程一次注入，也没有本轮逐课正文阅读。仅保存文件路径或界面完整输出不算模型已读全。入口只补这条实际读取规则及独立试验边界，没有继续增加营销Skill或运行设施。

这是有针对性方法资料、人工输入纠正及具体编辑反馈的同题返工，包含原有题目提示；不证明模型自主发现全部问题、不证明方法增量或跨题稳定性。根Lead没有代写v4/v5正文，编辑贡献完整留存。

记录到原生用量1,024,877 tokens：输入972,203（含缓存818,496），输出52,674（其中推理39,879）。中断中的请求可能有尚未回报用量；不含Codex/子agent用量，未查询人民币账单。重复输入和冗长推敲仍使这次小作业成本很高。未制作媒体、发布或开展真实营销效果验证。

机械核验见 [verification.json](verification.json)：旧v1–v3及题目哈希保持、v4/v5正文与原生结果及读稿快照一致，输入范围与截断单列。没有抓取供应商HTTP正文。两处启动器修改仅关闭自动记忆，Python语法、项目TOML解析、原生新进程配置读取和 `just fmt` 已通过；无产品Rust行为改动，不重复旧Rust测试。原分支和旧修改保留，未提交、合并或推送。

## 用户追加要求：关闭改造版 Codex 记忆

用户在 v5 返工进行中要求关闭自动记忆，以免影响每次试验效果判断。实际旧进程的 `config/read` 已返回 `features.memories=false`，源码默认值也为 false；不能据此声称此前稿件差是因为记忆开启。此前的 `generate_memories`、`use_memories` 未显式固定。

已在项目 `.codex/config.toml` 显式关闭 `features.memories`、`memories.generate_memories`、`memories.use_memories`；故事试验启动器与课程清洗启动器再用原生 `-c` 参数固定同样三项，避免隔离 CODEX_HOME 或其他配置层改变开关。未修改全局个人配置、删除记忆或旧成果。

新启动进程 `memory-off-check-20260909` 的实际 `config/read` 返回三项均为 false，来源为最高优先级 sessionFlags，项目层也已生效。见 [配置核验](memory-off-config-check.json)。探针只启动线程、读配置，未发模型 turn；已正常关闭。当前故事线程启动早于这次显式固定，记录为原功能开关已关闭的同题连续返工，不冒充重启后的独立试验。

长期记忆、同一任务历史和显式提供的课程/旧稿是三件事。后续独立试验使用新的原生线程和独立状态目录，仅提供预先列出的输入；同题返工可以带旧稿和反馈，但必须记为有辅助条件的返工。关闭记忆不证明消除了这些差异，也不证明相同 API 缓存命中就是内容泄漏。
